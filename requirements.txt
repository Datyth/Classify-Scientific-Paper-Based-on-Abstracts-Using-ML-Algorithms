# --- Core scientific stack (stable, fast) ---
numpy~=1.26.0
scipy~=1.11.0
pandas~=2.1.0
joblib~=1.3.0

# Match saved artifacts to avoid "InconsistentVersionWarning"
scikit-learn==1.6.1

# --- NLP / embeddings ---
# Torch: CPU by default; if you use CUDA, install the CUDA-specific wheel per PyTorch docs.
torch>=2.2,<2.5
transformers~=4.39.0
sentence-transformers~=2.6.1
sentencepiece>=0.1.99   # needed by some Transformer models

# --- Datasets / preprocessing ---
datasets~=2.19.0
nltk~=3.8.1

# --- App / Viz (optional but handy) ---
streamlit~=1.36
matplotlib~=3.8
seaborn~=0.13

# --- Optional: stratified multilabel split (if you re-enable it) ---
iterative-stratification>=0.1.7
